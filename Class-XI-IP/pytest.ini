[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
timeout = 60
# Separate markers so unit tests and e2e tests can be run independently
markers =
    e2e: end-to-end browser tests (require Playwright + running app)
    unit: fast unit tests (no browser, no server)
